package enums;

public enum Platforms {
        ANDROID("android"),
        IOS("ios");
        private String platform;
        Platforms(String platform) {
                this.platform = platform;
        }
}

